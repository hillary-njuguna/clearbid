# ClearBid Gate Pipeline
# Each gate is a cheap, deterministic veto filter.
# Gates run BEFORE expensive LLM reasoning (Reasoning Layer).
# A listing must pass ALL gates to reach the reasoning stage.
